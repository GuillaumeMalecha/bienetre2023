{% extends 'base.html.twig' %}

{% block title %}Ajouter un stage{% endblock %}

{% block body %}

    <div class="page-title">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 col-md-12">
                    <h2 class="ipt-title">Ajouter un stage</h2>
                    <span class="ipn-subtitle">Organisez un stage pour le prestataire {{ prestataire.nom }}</span>
                </div>
            </div>
        </div>
    </div>

    <br>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <h2>Ajouter un stage pour {{ prestataire.nom }}</h2>

                {{ form_start(form) }}
                {{ form_widget(form) }}

            </div>
        </div>
    </div>

{% endblock %}
